<?xml version="1.0"?>
<ruleset name="Technical Review PHPCS Rules">
    <description>PHP_CodeSniffer rules for Technical Review application</description>

    <!-- Analyze these directories -->
    <file>app/</file>
    <file>tests/</file>

    <!-- Exclude vendor and other directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>

    <!-- Use PSR-12 coding standard -->
    <rule ref="PSR12"/>

    <!-- Show progress and use colors -->
    <arg name="colors"/>
    <arg value="p"/>

    <!-- Configure specific rules -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>

    <!-- Allow longer method names in tests -->
    <rule ref="PSR1.Methods.CamelCapsMethodName.NotCamelCaps">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
</ruleset>
